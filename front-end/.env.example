# Cloudinary Configuration
# Get these values from your Cloudinary dashboard: https://cloudinary.com/console

# Your Cloudinary cloud name (required)
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name

# API Key (required for unsigned upload preset)
VITE_CLOUDINARY_API_KEY=your_api_key

# API Secret (optional for client-side, only needed if using SDK upload)
# Note: API Secret should ideally be kept on backend only for security
VITE_CLOUDINARY_API_SECRET=your_api_secret

# Upload Preset (required for unsigned uploads)
# Create an unsigned upload preset in Cloudinary dashboard:
# Settings > Upload > Upload presets > Add upload preset
# Set "Signing mode" to "Unsigned"
VITE_CLOUDINARY_UPLOAD_PRESET=your_upload_preset_name

# Google OAuth Configuration
# Get this from your Google Cloud Console: https://console.cloud.google.com/
VITE_GOOGLE_CLIENT_ID=130716148033-8ujfmbvjaa4p2oneti1k8end1dr16hne.apps.googleusercontent.com
# Gemini API Key
VITE_GEMINI_API_KEY=your_gemini_api_key
